# Changelog

PureAPI 遵循语义化版本风格管理版本号。

## 0.2.1

- 将仓库历史整理为单个初始提交。
- 将 Read the Docs 文档语言切换改为右下角浮动切换器。
- 使用新的包版本和 tag 发布本次更新。

## 0.2.0

- 将最低 Python 版本要求调整为 Python 3.11。
- 集成 Scalar API Reference，并将 `/docs` 作为默认文档入口。
- 保留 Swagger UI 文档入口 `/swagger`。
- 保留 ReDoc 文档入口 `/redoc`。
- 新增 GitHub Actions 发布流程，用于 tag 推送后自动构建并发布到 PyPI。
- 更新项目文档和 PyPI 元数据。

## 0.1.1

- 改进项目打包配置。

## 0.1.0

- 初始版本。
- 提供基础路由、请求对象、响应对象、异常处理和 OpenAPI 文档能力。
