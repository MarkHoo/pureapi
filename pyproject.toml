[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pureapi"
version = "0.2.1"
description = "A lightweight and elegant Python web framework"
readme = "README.md"
license = "MIT"
authors = [
    {name = "MarkHoo"}
]
keywords = ["web", "framework", "api", "rest", "openapi", "scalar", "swagger", "wsgi"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Web Environment",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Internet :: WWW/HTTP :: HTTP Servers",
    "Topic :: Internet :: WWW/HTTP :: WSGI :: Application",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]
requires-python = ">=3.11"
dependencies = []

[project.urls]
Homepage = "https://github.com/MarkHoo/pureapi"
Documentation = "https://github.com/MarkHoo/pureapi/tree/main/docs"
Repository = "https://github.com/MarkHoo/pureapi"
Issues = "https://github.com/MarkHoo/pureapi/issues"
PyPI = "https://pypi.org/project/pureapi/"
Changelog = "https://github.com/MarkHoo/pureapi/blob/main/CHANGELOG.md"
EnglishDocumentation = "https://github.com/MarkHoo/pureapi/blob/main/README.md"

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]
docs = [
    "sphinx>=7.0",
    "sphinx-rtd-theme>=2.0",
    "myst-parser>=2.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]

[tool.coverage.run]
source = ["src/pureapi"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
]
